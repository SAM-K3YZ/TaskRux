// Global state store — wire up Zustand or Redux Toolkit here
// Example Zustand setup (install with: npx expo install zustand):
//
// import { create } from 'zustand';
//
// type AuthState = {
//   token: string | null;
//   setToken: (token: string | null) => void;
// };
//
// export const useAuthStore = create<AuthState>((set) => ({
//   token: null,
//   setToken: (token) => set({ token }),
// }));

export {};
